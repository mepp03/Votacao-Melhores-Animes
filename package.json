{
  "dependencies": {
    "chokidar": "^3.6.0",
    "fs": "^0.0.1-security",
    "json-server": "^1.0.0-alpha.23",
    "mysql": "^2.18.1",
    "mysql2": "^3.2.4",
    "path": "^0.12.7"
  },
  "devDependencies": {
    "browserify": "^17.0.0"
  },
  "scripts": {
    "server": "json-server --watch dados/dbVotos.json --port 3000",
    "server-2019": "json-server --watch votos2019Inverno.json --port 3000",
    "server-2021": "json-server --watch votos2021Inverno.json --port 3000",
    "server-db": "json-server --watch db.json --port 3000",
    "start": "node server.js"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
